import React from "react";
import ReactDOM from "react-dom";

// hot-loaded by babel
// fork-ts-webpack-plugin
const App = () => {
  return <div>Personal Config!</div>;
};

ReactDOM.render(<App />, document.getElementById("root"));
